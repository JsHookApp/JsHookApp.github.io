v1.3.1:
- Android 16 compatibility
- Fixed a crash caused by native JSON parsing
- Communication link testing was adjusted to asynchronous
- Added an overlay framework, eliminating the need for (floating window permissions/rendering modules or client-side auxiliary processes).
- Integrated mcp_skills (tools->50+), allowing you to use AI to complete all hook tasks. AI can monitor device status and analyze applications in real time. Skills will be adjusted as needed before the official release.
- Added a dedicated kernel driver (not supported on some devices), only applicable to arm64 architecture kernel drivers, without any obfuscation/hiding.
- Fridamod updated with patches to adapt to the mcp environment.
- Kernel mode optimization.
- Activation optimization
- Overlay framework compatibility optimization
- Fix overlay modmenu rendering
- Fix nativelib import anomaly
- Skill enhancements
- Injector enhancements
- Frida integration (DexKit + r2frida + frida-il2cpp-bridge)
- Bug fixes and optimizations
- Bug fixes and optimizations.

Please update Fridamod to the latest version(1101) to adapt to the mcp service.

From this version onwards, you no longer need to research and write scripts yourself. Simply configure VS Code, submit your requirements to the AI, and all you need to do is accept the changes.

The documentation has been updated; you can view how to use mcp here:

https://doc.jshook.org